<?php

return array(

    /*
    |--------------------------------------------------------------------------
    | Login View
    |--------------------------------------------------------------------------
    |
    | This view will be used to render the login page. It can be changed to
    | show any view in your application. A basic form is provided by default.
    |
    */

    'view' => 'auth::login',

    /*
    |--------------------------------------------------------------------------
    | Redirect URL
    |--------------------------------------------------------------------------
    |
    | The route to be redirected to post authentication. By default this is the
    | base route of your application, but it can be customized to any route defined
    | in your routes.php.
    |
    */
    
    'redirect' => '/'

);